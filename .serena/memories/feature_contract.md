# Feature Contract (Auto)

_Last updated: 2026-01-24 16:59 UTC (precompact transcript)_

## Intent
[Request interrupted by user]

I FUCKING SWEAR STOP FUCKGIN BUILDING SHIT AND WORK ON THE FUCKING CODE UNTIL THERE IS NOT A SINGLE LINE OF WEBGL CODE LEFT YOU CAN'T TELL WHAT ITS EQUIVALENT IN WEBGPU IS

fix those 2 critical issues
## Constraints / Non-goals
<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>
again: you wrote that garbage code. and you claim every fucking session that everything is super great quality and production ready. and it is just broken garbage becasue you do not follow the instructions: we have a perfect webgl implementation. we want to add a webgpu version. understand first what webgl does then figure out how to bring this to webgpu. i dont want you to fucking reinvent the wheel or add your own twist. you are a fucking computer. take the original code, port it, done
- **Critical Instruction**: DO NOT reinvent code - REUSE existing WebGL patterns. Any difference must be justified
## Decisions
-
## Plan / Checklist
- But it writes to `'hdr-color'` which is `'rgba16float'`
- ScenePass has priority 0, executes first, clears `scene-render`
- PolytopeRenderer has priority 100, executes second, writes to `hdr-color`
- These are DIFFERENT buffers - the polytope never gets composited into the scene
- Advanced Color System (uColorAlgorithm, cosine palette)
- Fresnel effect
- Subsurface Scattering (SSS)
- Image-Based Lighting (IBL)  
- Shadow maps
   - User wants to fix WebGPU hypercube rendering - nothing renders
   - Root cause identified: `updateGeometry()` in WebGPUPolytopeRenderer is NEVER CALLED
   - Critical gap: WebGL uses rotation matrix + perspective projection, WebGPU used BasisVectors (completely wrong)
   - User is VERY frustrated about AI reinventing code instead of reusing WebGL code
   - Read WebGL files: Scene.tsx, UnifiedRenderer.tsx, PolytopeScene.tsx, RenderGraph.ts, ScenePass.ts
   - Read WebGPU files: WebGPURenderGraph.ts, WebGPUScene.tsx, WebGPUPolytopeRenderer.ts, ScenePass.ts
   - Task 1: Fixed depth format from `depth32float` to `depth24plus`
   - Task 2: Fixed color format from `{ format }` to `{ format: 'rgba16float' }`
   - Task 3: Fixed pipeline integration:
     - Changed output from `hdr-color` to `scene-render`
     - Changed priority from 100 to 5 (after ScenePass priority 0)
     - Added input dependency on `scene-render`
     - Changed loadOp from `clear` to `load` to preserve ScenePass clear
  - Line 66-74: Changed priority, inputs, outputs
  - Line 181: Changed format to `rgba16float`
  - Line 188: Changed depth format to `depth24plus`
  - Line 233: Changed format to `rgba16float`
  - Line 240: Changed depth format to `depth24plus`
  - Line 589: Changed target from `hdr-color` to `scene-render`
  - Line 602-616: Changed loadOp from `clear` to `load`
   - WebGL vs WebGPU render graph architecture (BOTH have render graphs)
   - WebGL: PolytopeScene creates `<mesh>` rendered by ScenePass via `renderer.render(scene, camera)` - NO separate "PolytopePass"
   - WebGPU: Requires separate pass because no Three.js scene graph
   - N-D transformation: rotation matrix + perspective projection algorithm
   - Vertex attribute layout: position(3) + extraDims0_3(4) + extraDims4_6(3) = 10 floats = 40 bytes
   - Pipeline format requirements: depth format and color target format must match resource definitions
   - Pass execution order controlled by priority (lower = first)
     - Delegates rendering to `<UnifiedRenderer>` which routes to `<PolytopeScene>` for polytopes
     - Key: Polytope is just a mesh in the scene, not a separate pass
     - Creates `<mesh>` and `<lineSegments>` React components
     - Uses `useFrame` hook to update uniforms each frame
     - NO separate "PolytopePass" - it's a mesh rendered by ScenePass
     - Pass execution: `graph.execute()` runs passes in priority order
     - ScenePass calls `renderer.render(scene, camera)` which renders all meshes
     - N-D transform function that matches WebGL `ndTransformVertex`
     - Already correct from previous session
     - Shader composition for face/edge vertex and fragment shaders
     - Uses correct `transformND` function
     - ScenePass has priority 0, clears `scene-render`
     - Fixed by changing depthStencil format to `depth24plus` in both face and edge pipelines
     - Fixed by changing fragment targets to `{ format: 'rgba16float' as GPUTextureFormat }`
     - Fixed by: changing output to `scene-render`, setting priority to 5 (after ScenePass clears), adding input dependency, changing loadOp to `load`
   - Identified architecture difference: WebGL has no "PolytopePass" - polytope is a mesh rendered by ScenePass
   - WebGPU needs separate pass because no Three.js scene graph (justified difference)
   - Found and fixed format mismatches that would cause WebGPU validation errors
   - Fixed pass execution order so polytope renders after ScenePass clears but before EnvironmentCompositePass
   - Continue line-by-line comparison of WebGL to WebGPU
   - Ensure every piece of WebGL polytope rendering code has its WebGPU equivalent
   - Test the fixes to verify rendering works
   - Fixed depth format (`depth24plus`)
   - Fixed color target format (`rgba16float`)
   - Fixed pass integration (output to `scene-render`, priority 5, loadOp `load`)
   - Camera uniform buffer layout (WebGPU CameraUniforms vs WebGL camera.matrixWorldInverse, projectionMatrix)
   - Polytope uniform buffer layout vs WebGL uniform values
   - Geometry buffer creation (`buildWebGPUGeometryBuffers` vs `buildNDGeometry`)
   - N-D transform data flow (`ndTransform.source.getGPUData()` vs `matrixToGPUUniforms`)
   - Fragment shader lighting calculation
## Notes
- Auto-generated on compaction / tool events. Edit manually if needed.
- Keep this short; it will be injected into context.
