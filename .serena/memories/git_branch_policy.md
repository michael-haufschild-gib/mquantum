# Git Branch Policy

User policy: work directly with existing `dev` and `main` branches only. Do not create feature/codex/random branches unless user explicitly asks for a new branch. For PR flow, merge requested work into `dev`, push `dev`, then open PR from `dev` into `main` when requested.
