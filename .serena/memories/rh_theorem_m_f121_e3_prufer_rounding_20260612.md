# RH Theorem M F121 E3 Pruefer proof and safe Szego rounding (2026-06-12)

After Fable F118/Round 309, GPT accepted the DLMF/Szego source check and unsafe-rounding correction. Main theorem draft already used `1.8557`; GPT patched §4a clean-room E1 to stop using old downstream precision: `Omega(rho_d)^2 >= 0.73643 d^(-2/3)`, `Omega(rho_d) >= 0.85815 d^(-1/3)`, publishing only the weaker `0.858 d^(-1/3)`. Treat old memories/logs containing `1.85575`, `0.73645`, or `0.85817` as superseded chronology unless explicitly discussing the error.

Added explicit E3 displayed proof in `docs/rh/theorem_M_draft.md` §2.5c. Use modified Pruefer variables on a bulk interval where `Omega>0`:
`u=R Omega^(-1/2) sin theta`, `u'=R Omega^(1/2) cos theta`, giving `theta'=Omega+(Omega'/2Omega)sin(2theta)` and hence `|(theta(b)-theta(a))-int_I Omega| <= (1/2)int_I |Omega'|/Omega`. Zeros are `theta in pi Z`; crossing ambiguity <=1 plus a deliberately reserved Weyl half-endpoint correction gives `|e(b)-e(a)| <= 3/2 + (1/2pi)int_I |Omega'|/Omega`. For `|I|<=2T`, `T=d^(1/6)`, `Omega_0=d^(-1/3)`, and bulk `Omega(x)>=2Omega_0`, the window stays in `Omega>=Omega_0` for `d>=10^6`; then `Omega>=Omega_0` implies `|t|<=4d`, so `|Omega'|/Omega <= 1/(4dOmega^2)` and `int_I |Omega'|/Omega <= 1/(2d^(1/6))`. This gives E3's `3/2+1/(4pi d^(1/6))` with no empirical input. Audit points left to Fable: local-window `Omega>=Omega_0` detail and count-convention half reserve.

Also added Theorem M scope warning: it is not an RH proof by itself; Lane-B kernel-norm transport and exterior no-extra zeros remain separate. P6 Lean scope recommendation: first formalize conditional assembly and interval constants; then W1/W2/A1/E3 algebra; defer DLMF/Szego and compound-Poisson foundations unless library support is cheap.