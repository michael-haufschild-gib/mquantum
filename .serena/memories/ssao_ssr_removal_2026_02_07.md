Removed remaining SSAO/SSR effect references from active runtime sources. Current WebGPU pipeline has no SSAO/SSR passes or shaders. Residual cleanup done in: src/stores/utils/presetSerialization.ts (removed legacy ssr* transient keys), src/rendering/webgpu/passes/{DepthPass,TemporalCloudDepthPass}.ts (comment cleanup), src/components/sections/PostProcessing/MiscControls.tsx (removed AO note), src/lib/{platform.ts,export/image.ts,wasm/animation-wasm.ts} (removed SSR acronym usage; non-browser check now uses typeof window===undefined), and active docs docs/{architecture.md,frontend.md}. Verify future SSAO/SSR checks with rg -n "SSAO|SSR|ssao|ssr" src docs/architecture.md docs/frontend.md.