/**
 * Gizmo Components Module
 *
 * Light gizmos are rendered via the WebGPU LightGizmoPass + DebugOverlayPass pipeline.
 * Click-to-select is handled in WebGPUScene's canvas overlay.
 *
 * @see src/rendering/webgpu/passes/LightGizmoPass.ts — GPU rendering
 * @see src/rendering/webgpu/passes/gizmoGeometry.ts — Geometry generation
 * @see src/rendering/webgpu/passes/DebugOverlayPass.ts — Compositing
 */
